#!/usr/bin/env python

from pymarkdown import render
from sys import argv

if not len(argv) == 2:
    print("Usage:\n\tpymarkdown myfile.md")

fn = argv[1]

with open(fn) as f:
    text = f.read()

output = render(text)

with open(fn, 'w') as f:
    f.write(output)
