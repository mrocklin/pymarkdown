#!/usr/bin/env python

from pymarkdown import process
from sys import argv

if not len(argv) == 2:
    print("Usage:\n\tpymarkdown myfile.md")

fn = argv[1]

with open(fn) as f:
    text = f.read()

output = process(text)

with open(fn, 'w') as f:
    f.write(output)
